{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port \"$PORT\"'",
    "aptPackages": [
      "libreoffice-writer",
      "libreoffice-impress",
      "fonts-dejavu-core"
    ]
  }
}
